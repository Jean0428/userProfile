// This script prompts the user to enter their full name, age, favorite number, and favorite color

// Prompt the user to enter their full name
let fullName = prompt("Enter your full name: ");

// Prompt the user to enter their age
let age = prompt("Enter your age: ");

// Prompt the user to enter their favorite number
let favNum = prompt("Enter your favorite number: ");

// Prompt the user to enter their favorite color
let favcol = prompt("Enter your favorite color: ");

// Log the user's full name to the console
console.log(fullName);

// Log the user's age to the console
console.log(age);

// Log the user's favorite number to the console
console.log(favNum);

// Log the user's favorite color to the console
console.log(favcol);